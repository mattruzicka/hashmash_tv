<h1>
  <%= form_tag("/tags/update") do %>
    <span class="hashmash">HashMash.tv /<span> 
    <%= submit_tag "watch", class: "btn btn-watch" %>
    <%= text_field_tag "tag", nil, placeholder: "#{@tag.present? ? @tag : "enter tag" }" %>
  <% end %>
</h1>
